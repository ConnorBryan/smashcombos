import React, { Component } from "react";

import { Layout } from "../components";

export default class AccountRequired extends Component {
  render() {
    return (
      <Layout>
        <p>AccountRequired</p>
      </Layout>
    );
  }
}
